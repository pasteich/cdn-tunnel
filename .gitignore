# Go build output
/cdntunnel
/cdn-host
*.test

# Android build outputs
android/.gradle/
android/build/
android/app/build/
android/local.properties
*.iml

# Regenerable native binary (build via README: CGO cross-compile of main.go)
android/app/src/main/jniLibs/**/libtun.so

# misc
.DS_Store

# Regenerable Linux server binaries (build: GOOS=linux go build; see android/README)
android/app/src/main/assets/server/cdn-tunnel-amd64
android/app/src/main/assets/server/cdn-tunnel-arm64

# Посторонняя сборка пакета (AUR), не часть проекта
/xray-bin/

# Готовый APK для раздачи (собирается: cd android && gradle :app:assembleDebug)
/dist/
